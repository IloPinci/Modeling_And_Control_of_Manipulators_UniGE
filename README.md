The evaluation of this assignment was 89/100.

The issues were:
  - Optimization Issue. \[In the *kinematicModel.m* it would be better if we just called a previous function instead of rewriting the same code to find the Jacobian of Tool. (Call the function of Jacobian of EE and then premultiply.)] 
  - In the main lamda should have been dynamic

These problems did not affect the correctness of the result. 
